import 'package:flutter/material.dart';

const String assetPath = 'assets/images';

SizedBox verSpace(double size) => SizedBox(
      height: size,
    );

SizedBox horSpace(double size) => SizedBox(
      width: size,
    );
