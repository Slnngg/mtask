import 'package:flutter/material.dart';

class QPayNavPage extends StatelessWidget {
  const QPayNavPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('qpay'),
    );
  }
}
