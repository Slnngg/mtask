import 'package:flutter/material.dart';

class ChatNavPage extends StatelessWidget {
  const ChatNavPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('chat'),
    );
  }
}
