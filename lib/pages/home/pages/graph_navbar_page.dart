import 'package:flutter/material.dart';

class GraphNavPage extends StatelessWidget {
  const GraphNavPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('graph'),
    );
  }
}
